
length = float(input("Enter the length (i): "))
width = float(input("Enter the width (w): "))
area, perimeter = compute_area_and_perimeter(length, width)
print(f"Area: {area}")
print(f"Perimeter: {perimeter}")
#write python to compute spead using s=ut+1/2a+2 using function

#write python program to calculate BMI (body
